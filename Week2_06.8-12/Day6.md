Picking up where I left off:

1. Today I continued troubleshooting Linux setup on one of the systems that has been unstable.
2. I focused on getting a clean, usable Linux environment so software install and build steps can run reliably.
3. I also worked through command and environment issues from earlier (package manager behavior, permissions, and command context), so I can separate setup problems from actual software problems.
4. At this point, the main blocker appears to be system-level configuration/boot behavior rather than just package or command syntax.
5. Because of that, I escalated to the office BIOS specialist to inspect firmware and hardware-level settings directly.
6. The BIOS specialist currently has the machine, and we are waiting on their findings and recommended changes.
7. While that is in progress, I documented the current state so we can resume quickly once the machine is returned.
8. Linux setup is still not complete on one computer.
9. BIOS/firmware review is in progress with office support.
10. Repeated software-level attempts were not producing a stable result.
11. The remaining symptoms suggested a lower-level issue, so BIOS-level review was the fastest way to reduce trial-and-error and avoid rework.

Next steps after I get the machine back:
13. Confirm Linux install stability and user access.
14. I'm able to ssh into the machine now, and its after 5, so I'm leaving. 